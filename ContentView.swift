import SwiftUI

struct ContentView: View {
    @State var answer = 0
    var body: some View {
        VStack {
            Button("Multiply"){
                
            }
            
            Text ("\(answer)")
            
        }
    }
}
